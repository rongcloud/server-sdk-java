apply plugin: 'java'

repositories {
    mavenCentral()
}

jar {
    baseName = 'rongyun-server-sdk'
    version = '0.1.0'
}

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}